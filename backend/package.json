{
  "name": "todo-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "init-db": "node init-db.js",
    "start": "node dist/index.js",
    "test-db": "node -e \"const db = require('better-sqlite3')('./todo.db'); console.log('✅ Database connected successfully'); const tables = db.prepare(\\\"SELECT name FROM sqlite_master WHERE type='table'\\\").all(); console.log('📋 Tables:', tables.map(t => t.name)); db.close();\""
  },
  "dependencies": {
    "@trpc/server": "^10.45.0",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  }
}
